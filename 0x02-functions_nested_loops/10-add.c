/**
 * add - this function adds two ints;
 * @x: this is the first parameter;
 * @y: this is the second parameter;
 * Return: it returns an int type sum;
 */
int add(int x, int y)
{
int sum = x + y;
return (sum);
}
